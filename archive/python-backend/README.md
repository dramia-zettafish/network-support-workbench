# Archived Python Backend

This folder preserves the previous FastAPI, SQLAlchemy, and Alembic backend for short-term reference.

It is not part of the normal local runtime. The active app runs through:

- Next.js frontend
- Next.js API routes
- PostgreSQL initialized from `db/network_vcode_schema.sql`

